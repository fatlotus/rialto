# This file is only the default; please change the
# settings here before uploading to Google App Engine.

SESSION_SECRET = "" # Set this value to a random string.
  
  # On a UNIX system, this can be done most easily by
  # running the following at a command line:
  # 
  # dd if=/dev/urandom count=10240 bs=1024 | openssl sha1